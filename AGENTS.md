## Agent skills

### Issue tracker

Issues are tracked in GitHub Issues for this repo using the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

This repo uses only `ready-for-agent` and `ready-for-human` from the triage label vocabulary. See `docs/agents/triage-labels.md`.

### Domain docs

This repo uses a single-context domain documentation layout. See `docs/agents/domain.md`.
